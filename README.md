# SwiftUICentered

## Instalation

To install this package, import `url here` in SPM

## Usege example

```swift

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Centered")
            .centerHorizontally()
        }
    }
}
```
